params {
    fastq     = "$PWD/fastq"
    fast5     = "$PWD/fast5"
    reference = "$PWD/reference/ref.fa"
    config    = "$PWD/reference/repeat_config.tsv"
    outdir    = "./results"
}
docker.enabled = true
